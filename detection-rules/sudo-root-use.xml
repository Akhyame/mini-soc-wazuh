<!-- Mini SOC local rule 100102: tested on Wazuh 4.14.7, 2026-09-22.
     Rule fragment only: insert inside the existing local <group> in
     /var/ossec/etc/rules/local_rules.xml; do not replace the entire file.
     Detects authorized OR unauthorized sudo-to-root use; not proof of attack. -->
<rule id="100102" level="5">
  <if_sid>5402</if_sid>
  <description>Mini SOC: sudo command executed as root - review context.</description>
  <group>mini_soc,privilege_use,</group>
</rule>
